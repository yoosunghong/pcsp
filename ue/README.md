# Unreal Project Placeholder

Create or place the Unreal Engine project in this directory.

Common Unreal-generated directories such as `Binaries/`, `Intermediate/`,
`Saved/`, and `DerivedDataCache/` are ignored at the repository root.
